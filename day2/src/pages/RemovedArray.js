const RemovedArray = ({RemovedList})=>{
    return (
        <>
            {/* Removed Page
            {
            RemovedList.map((task)=> {
                return (

                )
            })
            } */}
        </>
    )
}

export default RemovedArray;